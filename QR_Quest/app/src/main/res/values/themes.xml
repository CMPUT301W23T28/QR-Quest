<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProjectQR" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#CB8932</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">#CB8932</item>
        <!-- Customize your theme here. -->
        <item name="android:editTextColor">@android:color/black</item>
        <item name="android:textColorHint">#605F5F</item>
    </style>

    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:textColor">#000000</item>
        <item name="android:textColorHint">#777777</item>
        <item name="android:hint">"Search Here"</item>
        <item name="searchIcon">@null</item>
        <item name="queryBackground">@drawable/rounded_search_bar</item>
        <item name="submitBackground">@drawable/rounded_search_bar</item>
    </style>
</resources>
